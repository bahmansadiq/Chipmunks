# insidet the html body there are elements:
form which represent a form to the page which consist of all the following elements.

fieldset : this tag brings boundry for the sub tags and separate them from other portion of the form
div : divide the fieldset to sub divistion to be more specific in each element and label and input types are used inside div tag;

label: labe the tage after it for giving name of the following tag
input types: text, date, name, email checkbox to collect different types of information from the user.
button type submit to submit the result
